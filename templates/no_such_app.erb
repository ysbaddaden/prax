<html>
  <head>
    <title>No such application <%= app.name %></title>
  </head>
  <body>
    <h1>No such application <q><%= app.name %></q></h1>
    <p>Please link <q><%= app.name %></q> to <code><%= Prax::Config.host_root %></code></p>

    <h2>Example:</h2>

<pre><code>$ ln -s ~/Work/<%= app.name %> <%= Prax::Config.host_root %></code></pre>
  </body>
</html>
