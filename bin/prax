#!/usr/bin/env ruby

require File.expand_path("../../lib/prax", __FILE__)


# Kill any existing copies:
pids = `ps x|grep praxd|grep -v grep|awk "{print \\$1}"`.split
  pids.each do |pid|
    if pid != Process.pid
      Process.kill(:KILL, pid)
    end
  end


Prax::CLI.run
